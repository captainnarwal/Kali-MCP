# Prefer installing the package (pulls deps from pyproject.toml):
#   pip install -e .
#
# This file mirrors those dependencies for `pip install -r requirements.txt`.

mcp>=1.9.0
python-dotenv>=1.0.0
uvicorn>=0.30.0
starlette>=0.37.0
httpx2>=0.1.0
httpx>=0.27.0
