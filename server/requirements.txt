# Kali MCP server — Streamable HTTP (no LLM)
mcp>=1.9.0
python-dotenv>=1.0.0
uvicorn>=0.30.0
starlette>=0.37.0
# Prefer httpx2 when available (mcp SDK / transport); httpx is a fallback
httpx2>=0.1.0
httpx>=0.27.0
