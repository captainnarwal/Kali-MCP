# Kali MCP client — LLM agent + MCP Streamable HTTP client
mcp>=1.9.0
python-dotenv>=1.0.0
httpx>=0.27.0
httpx2>=0.1.0

# LLM providers (install all; only the configured provider is used at runtime)
anthropic>=0.40.0
openai>=1.50.0
mistralai>=1.0.0
google-genai>=1.0.0
ollama>=0.3.0
